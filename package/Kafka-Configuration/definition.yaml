## WARNING: This file was autogenerated!
## Manual modifications will be overwritten
## unless ignore: true is set in generate.yaml!
## Last Modification: 17:02:31 on 12-18-2023.

apiVersion: apiextensions.crossplane.io/v1
kind: CompositeResourceDefinition
metadata:
  name: compositeconfigurations.kafka.aws.example.cloud
spec:
  claimNames:
    kind: Configuration
    plural: configurations
  defaultCompositionRef:
    name: configuration
  group: kafka.aws.example.cloud
  names:
    categories:
    - crossplane
    - composition
    - kafka
    kind: CompositeConfiguration
    plural: compositeconfigurations
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    name: v1alpha1
    referenceable: true
    schema:
      openAPIV3Schema:
        properties:
          spec:
            description: ConfigurationSpec defines the desired state of Configuration
            properties:
              deletionPolicy:
                default: Delete
                description: DeletionPolicy specifies what will happen to the underlying
                  external when this managed resource is deleted - either "Delete"
                  or "Orphan" the external resource.
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                description: ConfigurationParameters defines the desired state of
                  Configuration
                properties:
                  description:
                    description: The description of the configuration.
                    type: string
                  kafkaVersions:
                    description: The versions of Apache Kafka with which you can use
                      this MSK configuration.
                    items:
                      type: string
                    type: array
                  properties:
                    description: kafka server configurations
                    items:
                      type: string
                    type: array
                  region:
                    description: Region is which region the Configuration will be
                      created.
                    type: string
                required:
                - properties
                - region
                type: object
              providerConfigRef:
                description: ProviderConfigReference specifies how the provider that
                  will be used to create, observe, update, and delete this managed
                  resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: PublishConnectionDetailsTo specifies the connection secret
                  config which contains a name, metadata and a reference to secret
                  store config to which any connection details for this managed resource
                  should be written. Connection details frequently include the endpoint,
                  username, and password required to connect to the managed resource.
                properties:
                  configRef:
                    description: SecretStoreConfigRef specifies which secret store
                      config should be used for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are the annotations to be added to
                          connection secret. - For Kubernetes secrets, this will be
                          used as "metadata.annotations". - It is up to Secret Store
                          implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels are the labels/tags to be added to connection
                          secret. - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store
                          types.
                        type: object
                      type:
                        description: Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: ConfigurationStatus defines the observed state of Configuration.
            properties:
              atProvider:
                description: ConfigurationObservation defines the observed state of
                  Configuration
                properties:
                  arn:
                    description: The Amazon Resource Name (ARN) of the configuration.
                    type: string
                  creationTime:
                    description: The time when the configuration was created.
                    format: date-time
                    type: string
                  latestRevision:
                    description: Latest revision of the configuration.
                    properties:
                      creationTime:
                        format: date-time
                        type: string
                      description:
                        type: string
                      revision:
                        format: int64
                        type: integer
                    type: object
                  name:
                    description: The name of the configuration. Configuration names
                      are strings that match the regex "^[0-9A-Za-z-]+$".
                    type: string
                  state:
                    description: The state of the configuration. The possible states
                      are ACTIVE, DELETING and DELETE_FAILED.
                    type: string
                type: object
              observed:
                description: Freeform field containing information about the observed
                  status.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              uid:
                description: The unique ID of this Configuration resource reported
                  by the provider
                type: string
            type: object
    served: true
